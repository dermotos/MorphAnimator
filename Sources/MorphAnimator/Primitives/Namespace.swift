//
//  Namespace.swift
//

/// Namespace for types used by the MorphAnimator view controller transition system.
///
/// In order to keep naming of MorphAnimator types concise within the larger StyleKit framework, most types are nested inside this enum. The exception is protocols, whose names are prefixed `Morph`.
///

public enum Morph { }
